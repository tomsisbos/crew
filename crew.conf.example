# crew configuration — copy to ~/.config/crew/crew.conf and edit.
# Every value below is also the built-in default, so an empty config is fine.

# Name of the agmsg team all crew agents join.
TEAM="crew"

# Installed agmsg command name (the --cmd you passed to agmsg's install.sh).
# Determines the script path ~/.agents/skills/<AGMSG_CMD>/scripts.
AGMSG_CMD="agmsg"

# Command launched in each worktree pane. Override per-run with CREW_AGENT_CMD,
# e.g. to pass flags: CREW_AGENT_CMD="claude --model opus" crew new ...
AGENT_CMD="claude"

# Which herdr agent_status transitions the bridge announces to teammates.
# Space-separated subset of: blocked done idle working
#   blocked = agent is waiting for input
#   done    = agent finished its turn and hasn't been viewed (what `herdr pane
#             list` reports for a finished agent — the usual "free now" signal)
#   idle    = finished AND already viewed (rarely seen by a headless bridge)
#   working = agent started working (noisy; off by default)
BRIDGE_TRANSITIONS="blocked done"

# How often (seconds) the bridge polls herdr for state changes.
BRIDGE_INTERVAL="3"

# Echo-suppression window (seconds). In monitor mode, a delivered note wakes an agent,
# which flips it working -> done; the bridge ignores such a transition if it lands within
# this many seconds of the bridge pinging that agent, so notes don't reverberate forever.
# Larger = safer against slow reaction turns; smaller = announces genuine changes sooner.
# (blocked is always announced, regardless of this window.)
BRIDGE_ECHO_WINDOW="60"

# How often (seconds) `crew watch` polls herdr for new worktrees to adopt.
WATCH_INTERVAL="3"

# Whether `crew watch` shows a herdr toast when you create a plain (non-worktree)
# workspace on the main checkout — a reminder that only worktrees become crew agents.
# Set to "0" to silence it.
WATCH_NUDGE="1"
